# data.yaml

# 1. 数据集根目录 (绝对路径)
path: /root/escap_train/dataset_cone

# 2. 训练集和验证集的相对路径
train: train/images
val: val/images

# 3. 类别数量 (只有雪糕筒一种)
nc: 1

# 4. 类别名称 (必须是英文，对应 ID 0)
names: ['cone']